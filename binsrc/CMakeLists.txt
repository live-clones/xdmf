ADD_EXECUTABLE(XdmfFormatExample XdmfFormatExample.cxx)

LINK_DIRECTORIES(../libsrc)
LINK_LIBRARIES(Xdmf)
LINK_LIBRARIES(hdf5 expat)
IF(XDMF_HAVE_NDGM)
	LINK_LIBRARIES(ndgm)
	LINK_LIBRARIES(${CMAKE_DL_LIBS})
ENDIF(XDMF_HAVE_NDGM)
LINK_LIBRARIES(z)

IF(ICE_INSTALLED)
		INSTALL_TARGETS(${ICE_ROOT}/Release${ICE_VERSION}/${ARCH_TO_BUILD}/bin XdmfFormatExample)
ENDIF(ICE_INSTALLED)
